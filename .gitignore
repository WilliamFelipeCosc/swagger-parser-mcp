.venv
__pycache__
.env

*.ignore.*
data/

# uv's project lockfile — generated by `uv sync` / `fastmcp run`, and never
# consulted when this package is installed (uvx/pip resolve from pyproject.toml).
# See the library-vs-application lockfile convention.
uv.lock

# build artifacts
build/
dist/
*.egg-info/
